emulate -LR zsh

zmodload zsh/zutil

setopt extendedglob typesetsilent warncreateglobal

bindkey -e

local REPLY
local -a reply

builtin autoload -Uz colors
colors

# FUNCTION: SetMessage {{{
SetMessage() {
    echo "$1"
}
# }}}

# vim:ft=zsh
